// Bapx's conversation message types are owned by @bapX/sdk and consumed
// directly here — React adds optimistic/local state but does not reinterpret
// canonical part semantics.
export type { BapxConversationMessage, BapxConversationPart } from '@bapX/sdk';
