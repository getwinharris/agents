{
	"name": "zendesk-channel-example",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bapX build --target node",
		"build:cloudflare": "bapX build --target cloudflare",
		"check:types": "tsc --noEmit",
		"test": "vitest run --config vitest.config.ts && vitest run --config vitest.workerd.config.ts"
	},
	"dependencies": {
		"@bapX/runtime": "*",
		"@bapX/zendesk": "*",
		"lossless-json": "*"
	},
	"devDependencies": {
		"@bapX/cli": "*",
		"@cloudflare/vitest-pool-workers": "0.16.20",
		"@types/node": "^22.10.10",
		"agents": "^0.14.2",
		"typescript": "^6.0.3",
		"vitest": "^4.1.6"
	}
}
